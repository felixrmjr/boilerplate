import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
	appId: "com.example.app",
	appName: "nomeaqui",
	webDir: "dist/nomeaqui",
	bundledWebRuntime: false,
};

export default config;
